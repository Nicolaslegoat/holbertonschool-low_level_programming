#include <stdio.h>
#include <stdlib.h>

/**
*  main - Description pour main
*  Return: Always 0 (succes)
*/

int main(void)
{
	char ch = 'a';
	fot (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
	return (0);
}

#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Description pour main
 * Return: Always 0 (succes)
 */

int main(void)
{
	char a;

	for (a = 'z'; a <= 'a'; a++)
	{
		putchar(a);
	}
	{
		putchar('\n');
	}
	return (0);
}
